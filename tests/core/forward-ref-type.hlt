#
# @TEST-EXEC:  hiltic %INPUT -p -o out.ll
# @TEST-EXEC:  btest-diff out.ll

module Main

import Hilti

global ref<A> a
global ref<B> b
global ref<list<ref<A>>> v
global tuple<ref<A>, ref<B>> t

type myException = exception<ref<A>>

ref<A> foo(ref<B> b) {
    return.result a
}

type A = struct {
    ref<A> a,
    ref<B> b
    }

type B = struct {
    ref<A> a,
    ref<B> b,
    ref<A> c,
    ref<B> d
    }

void  run() { }
