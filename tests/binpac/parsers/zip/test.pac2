#
# @TEST-EXEC:  cat %DIR/archive.zip.dat | pac-driver-test %INPUT -I ${PARSERS} ${PARSERS}/mstime.pac2 ${PARSERS}/zip.pac2 -- -p ZIP::Archive >output
# @TEST-EXEC:  btest-diff output
#

module Test;

import parsers/zip;

on ZIP::File::%done {
    print self;
    }

