# $Id$
#
# @TEST-EXEC:  hilti-build -d %INPUT ${PAC_DRIVER} -o a.out
# @TEST-EXEC:  echo 1234567890abcdef | ./a.out >output
# @TEST-EXEC:  btest-diff output

module Mini;

export type test = unit {

       a: bytes &length=4
          { print "A"; }

       b: bytes &length=6
          { print self.b; }

       c: bytes &length=6
          { print self; }
};
