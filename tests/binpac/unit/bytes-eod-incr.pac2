#
# @TEST-EXEC:  hilti-build -d %INPUT ${PAC_DRIVER} -o a.out
# @TEST-EXEC:  echo 1234567890 | HILTI_DEBUG=$HILTI_DEBUG:binpac ./a.out -i 1 >output
# @TEST-EXEC:  btest-diff output

module Mini;

export type test = unit {
       a: bytes &length=4;
       b: bytes &eod
          { print self.a, self.b; }
};
