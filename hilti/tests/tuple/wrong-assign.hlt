# $Id$
#
# @TEST-EXEC-FAIL: hilti-build %INPUT a.out >output 2>&1 
# @TEST-EXEC:      test-diff output
#
# Some broken assigns. All are supposed to fail.

module Main

void run() {
    local tuple<int64, bool> a
    
    a = tuple.assign 42
}

# @TEST-START-NEXT

module Main

void run() {
    local tuple<int64, bool> a
    local tuple<int64, string> b
    
    a = tuple.assign b
}

# @TEST-START-NEXT

module Main

void run() {
    local tuple<int64, bool> a
    local tuple<int64, string> b
    
    a = tuple.assign (1,2)
}
