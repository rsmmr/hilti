#! /usr/bin/env bash 

pac=$1

export CFLAGS=`binpac-config --cflags`
export HILTICFLAGS=`binpac-config --hiltic`

if [ "$pac" != "" ]; then
   pac_hlt=$pac.hlt
   binpac++ $pac -o $pac_hlt || exit 1
fi

hilti-build -a -o pac-driver pac-driver.c $pac_hlt


