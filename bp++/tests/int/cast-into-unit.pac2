# $Id$
#
# @TEST-EXEC:  hilti-build2 -d -m %INPUT ${PAC_DRIVER} -o a.out
# @TEST-EXEC:  printf '\x00\x05WORKS' | HILTI_DEBUG=binpac ./a.out -p Mini::test >output 2>&1
# @TEST-EXEC:  btest-diff output

module Mini;

import BinPAC;

type MyUnit = unit(s: uint64) {
    b: bytes &length = s
        { print self.b; }
};

export type test = unit {
    a: uint16;
    text: MyUnit(self.a);
};

