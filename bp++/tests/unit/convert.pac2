# $Id$
#
# @TEST-EXEC:  hilti-build -m %INPUT ${PAC_DRIVER} -o a.out
# @TEST-EXEC:  echo AaBb567890 | ./a.out -p Mini::test >output 2>&1
# @TEST-EXEC:  btest-diff output

module Mini;

# These are implemented in libhilti.
extern int64 Hilti::bytes_to_int(b: bytes);
extern bytes Hilti::bytes_to_lower(b: bytes);

bytes to_lower(b: bytes)
{
     return Hilti::bytes_to_lower(b);
}

int64 to_int(b: bytes)
{
     return Hilti::bytes_to_int(b);
}

export type test = unit {
       a: bytes &length=4 &convert=to_lower;
       b: bytes &length=6 &convert=to_int
          { print self.a; print self.b; }
};
