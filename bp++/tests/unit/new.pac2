# $Id$
#
# @TEST-EXEC:  hilti-build -d -m %INPUT ${PAC_DRIVER} -o a.out
# @TEST-EXEC:  echo 1234567890 | ./a.out -p Mini::Test >output
# @TEST-EXEC:  echo 1234567890 | ./a.out -p Mini::Test2 >output
# @TEST-EXEC:  btest-diff output

module Mini;

export type Test = unit {
       a: bytes &length=4;
       b: bytes &length=6
          {
          local x: Test;
          x = new Test;
          print x;
          }
};

export type Test2 = unit(a: bytes, b: bool) {
       c: bytes &length=4;
       d: bytes &length=6
          {
          local x: Test2;
          x = new Test2(b"123", True);
          print x;
          }
};
