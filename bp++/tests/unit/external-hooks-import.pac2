# $Id$
#
# @TEST-EXEC:  hilti-build2 -m %INPUT ${PAC_DRIVER} -o a.out
# @TEST-EXEC:  echo 1234567890 | ./a.out >output 2>&1
# @TEST-EXEC:  btest-diff output

module test;

import mini;

on mini::test::a { print "FOO a", self.a; }
on mini::test::b { print "FOO b", self.b; self.foo = b"x"; }
on mini::test::foo { print "FOO foo", self.foo; }
on mini::test::%init { print "FOO init"; }

### @TEST-START-FILE mini.pac2

module Mini;

export type test = unit {

       a: bytes &length=4;
       b: bytes &length=6;

       var foo: bytes;
};

### # @TEST-END-FILE
