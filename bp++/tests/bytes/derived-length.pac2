# $Id$
#
# @TEST-EXEC:  hilti-build -m %INPUT ${PAC_DRIVER} -o a.out
# @TEST-EXEC:  echo "\x01\x00\x02ABC" | ./a.out -p Mini::test >output 2>&1
# @TEST-EXEC:  btest-diff output

module Mini;

export type test = unit {
       a: uint8;
       b: uint16;
       c: bytes &length = self.a+self.b
           { print self.c; }
};
