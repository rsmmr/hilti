# $Id$
#
# @TEST-EXEC-FAIL: binpac++ %INPUT >output 2>&1
# @TEST-EXEC:      test-diff output

module HTTP;

const LINE = /[^\n]+\n/;

type Item = unit {
    line:   LINE;
};

type Test = unit {
    %export True;
    lines: list<Item>;
    end: LINE;
    };

